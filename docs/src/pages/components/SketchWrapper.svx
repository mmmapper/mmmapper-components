<script>
  import { Screen, SketchWrapper } from '@mmmapper/components';
</script>

# SketchWrapper

<Screen
  class="border shadow mx-auto"
  editMode={true}
  width="600"
  height="400">
  <SketchWrapper>
  </SketchWrapper>
</Screen>

```
<script>
  import { Screen, SketchWrapper } from '@mmmapper/components';
</script>

<Screen
  class="border shadow mx-auto"
  editMode={true}
  width="600"
  height="400">
  <SketchWrapper>
  </SketchWrapper>
</Screen>
```